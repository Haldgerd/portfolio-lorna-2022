import React from 'react';
import styles from './banner.module.css';

const About: React.VFC = (): JSX.Element => {
    return <section className={`${styles.banner} tw-h-20 tw-bg-red-500`} />;
};

export default About;
